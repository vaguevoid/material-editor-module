{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch Engine - Debug",
            "preLaunchTask": "Build + Copy",
            "program": "C:/projects/void/engine/target/debug/platform_native",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
